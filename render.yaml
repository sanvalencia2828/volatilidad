services:
  - type: web
    name: sistema-volatilidad
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn backend:app -w 1 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT
